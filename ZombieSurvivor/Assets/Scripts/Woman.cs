using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Woman : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Academy Home!!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
